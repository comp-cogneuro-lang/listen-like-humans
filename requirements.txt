# Tested with Python 3.10–3.13 (e.g. torch 2.6, transformers 4.45, numpy 2.x).
# Version floors are lower bounds, not exact pins — newer releases generally work.

# Core (training + testing the from-scratch models)
torch>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
librosa>=0.10
soundfile>=0.12
textgrid>=1.5
gensim>=4.3
joblib>=1.3
tqdm>=4.65
wandb>=0.16          # optional logging; disable with USE_WANDB=0

# Plotting / figure notebooks (also needs Jupyter: pip install jupyter)
matplotlib>=3.7
seaborn>=0.12
plotly>=5.20
kaleido>=0.2

# Foundation-model evaluation (wav2vec 2.0, HuBERT, Whisper)
transformers>=4.40

# Optional — only for the (unreported) Nemotron eval in pretrained_models/
# nemo_toolkit
# omegaconf
